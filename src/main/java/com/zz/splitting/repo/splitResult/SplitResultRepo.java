package com.zz.splitting.repo.splitResult;

import com.zz.splitting.entity.splitResult.SplitResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SplitResultRepo extends JpaRepository<SplitResult, Integer> {
}
